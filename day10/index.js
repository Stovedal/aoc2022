const newLine = "\n"

const sumSignalStrengthsAt = ({ instructions, cycles }) => {
  instructions = instructions.split(newLine)
  
  // instructions.reduce(())

}

module.exports = {
  compute: sumSignalStrengthsAt,
  sumSignalStrengthsAt
}
